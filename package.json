{
  "name": "eip-pixel11xl-forge",
  "version": "0.1.0-rc.6",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "node --test --test-concurrency=2 tests/*.test.mjs",
    "check": "npm test && bash tools/check-public-tree.sh"
  }
}
